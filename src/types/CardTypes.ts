export type cardsSumType = {
  sum: number;
  visualSum: string;
};

export type usedCardType = {
  cardSymbol: string;
  value: number;
  visualValue: string;
  HiLoValue: number;
};

export type deckCardType = {
  value: number;
  visualValue: string;
  HiLoValue: number;
};
